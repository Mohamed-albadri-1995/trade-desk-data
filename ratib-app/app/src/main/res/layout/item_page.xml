<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingHorizontal="20dp"
    android:paddingVertical="14dp">

    <TextView
        android:id="@+id/pageText"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="top"
        android:textSize="10sp"
        android:textColor="@color/reading_text"
        android:fontFamily="@font/amiri"
        android:lineSpacingMultiplier="1.3"
        android:justificationMode="inter_word"
        android:hyphenationFrequency="none"
        tools:targetApi="26" />

    <!-- Footnote pinned to the bottom (shown only on pages that have one). -->
    <View
        android:id="@+id/footnoteRule"
        android:layout_width="140dp"
        android:layout_height="1dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="6dp"
        android:background="@color/footnote_text"
        android:visibility="gone" />

    <TextView
        android:id="@+id/footnoteText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:fontFamily="@font/amiri"
        android:lineSpacingMultiplier="1.3"
        android:textColor="@color/footnote_text"
        android:visibility="gone" />
</LinearLayout>
