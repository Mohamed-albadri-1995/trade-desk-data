pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // JitPack hosts the PDF viewer the book is read through.
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "DalailAlRahamat"
include ':app'
